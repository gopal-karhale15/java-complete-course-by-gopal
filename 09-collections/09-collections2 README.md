# Collections

## Theory
Collections are used to store and manage data dynamically in Java.

## Types
- ArrayList → ordered list
- HashMap → key-value pairs
- HashSet → unique elements (no duplicates)
